#include <iostream>

using namespace std;

int Mod(int n, int m)
{
    int x = 28;
    for (int i = 0; i <)
        return x % m;
}
int main()
{
    int n, m;
    cin >> n >> m;
    cout << Mod(n, m);
    return 0;
}